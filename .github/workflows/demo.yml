# Builds this repository's own gallery with the action in this checkout.
# Doubles as the end-to-end test: if this is red, do not move the major tag.
name: Demo site

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: ./
      - uses: actions/upload-pages-artifact@v5
        with:
          path: public

  deploy:
    needs: build
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      # Fails with a clear message when Pages has not been switched to the
      # "GitHub Actions" source yet, instead of letting deploy-pages fail later.
      - uses: actions/configure-pages@v6
      - id: deployment
        uses: actions/deploy-pages@v5
